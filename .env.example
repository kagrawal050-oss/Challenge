NEXT_PUBLIC_APP_NAME=Culture Compass

# Optional: set these to use the OpenAI Responses API from /api/travel-plan.
# Without OPENAI_API_KEY, the app uses its local fallback generator.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
